syntax = "proto3";

package auth;

service AuthService {
    rpc Authenticate (Authentication) returns (UserMessage){}
}

message Authentication{
    string Authentication = 1;
}

message UserMessage{
    string userId = 1;
    string email = 2;
}