export enum Actions {
  ADD = 'add',
  EDIT = 'edit',
  DELETE = 'delete',
  READ = 'read',
}

export enum Subjects {
  PRODUCTS = 'products',
  USERS = 'users',
  ROLES = 'roles',
  PERMISSIONS = 'permissions',
}
